{
  "entities": {
    "User": {
      "title": "User",
      "description": "A user of the flashcard app.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "displayName": { "type": "string", "description": "User's display name" },
        "photoURL": { "type": "string", "description": "User's profile picture URL" },
        "createdAt": { "type": "timestamp", "description": "When the user account was created" }
      },
      "required": ["uid", "createdAt"]
    },
    "LevelProgress": {
      "title": "LevelProgress",
      "description": "Tracks a user's progress in a specific level.",
      "type": "object",
      "properties": {
        "viewedCards": { 
          "type": "array", 
          "items": { "type": "string" },
          "description": "Array of flashcard IDs the user has viewed in this level" 
        },
        "lastAccessed": { "type": "timestamp", "description": "When the user last studied this level" }
      },
      "required": ["viewedCards", "lastAccessed"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Stores user profile information."
    },
    "/users/{userId}/progress/{levelId}": {
      "schema": "LevelProgress",
      "description": "Stores user progress for a specific level (beginner, intermediate, advanced)."
    }
  }
}
